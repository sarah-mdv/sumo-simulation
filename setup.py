from setuptools import setup

setup(name='sumo-simulation',
      version='0.1',
      description='Easy data retrieval interface from sumo simulation',
      author='Sarah Moy de Vitry',
      license='MIT',
      packages=['src'],
      zip_safe=False)
